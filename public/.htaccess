<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On

    # Route all requests to index.php if they are not existing files/directories
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
</IfModule>
